#include <.h>

 name(/* arguments */) {
  /* code */
  return 0;
}


int main(int argc, char const *argv[]) {
  printf("%s\n", );

  for (size_t i = 0; i < count; i++) {
    while (not worg) {
      for (size_t i = 0; i < count; i++) {
        do {

        } while(not worg);
      }
    }
  }





  return 0;
}
